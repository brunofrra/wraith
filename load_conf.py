# load_conf.py

# Handles loading the config and generating the configuration object
import toml

# TODO: Create configuration class

def load (filename):
    # TODO load the toml and build the object
    return toml.load (filename)
